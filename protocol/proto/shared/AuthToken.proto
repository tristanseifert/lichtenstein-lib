syntax = "proto3";
package lichtenstein.protocol;

/**
 * An authentication token that identifies and authorizes the device making the
 * request.
 */
message AuthToken {
    // auth token
    bytes token = 1;
}